
// export const greetUser = users => users.map(user => console.log(user))

const greetUser = users => users.map(user => console.log(user))

export default greetUser;

export const addFunc = (a, b) => console.log(a + b);
